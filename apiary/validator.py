"""This module defines the Validators used within the CoopHive protocol."""

from apiary.base_agent import Agent


class PlaceholderValidator(Agent):
    """A Validator in the CoopHive protocol."""

    def __init__():
        """Initialize the Validator instance."""
        super().__init__()

    def infer(self, states, input_message):
        """Policy of Placeholder Validator."""
        pass
