[tool.poetry]
name = "coophive-simulator"
version = "0.1.0"
description = "A simulator for the game theory of the CoopHive protocol"
authors = ["Levi Rybalov"]
license = "Apache 2"
readme = "README.md"
packages = [{include = "coophive_simulator"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
